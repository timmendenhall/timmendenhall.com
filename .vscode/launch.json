{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: dev",
            "request": "launch",
            "command": "pnpm run dev",
            "cwd": "${workspaceFolder}"
        }
    ]
}
